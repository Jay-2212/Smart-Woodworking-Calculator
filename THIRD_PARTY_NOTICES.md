# Third-Party Notices

This project vendors the following third-party libraries under `libs/`:

- **Three.js** (`libs/three.min.js`) — Copyright 2010-2023 Three.js Authors, MIT License. https://github.com/mrdoob/three.js
- **OrbitControls** (`libs/OrbitControlsGlobal.js`) — adapted from Three.js's OrbitControls example (same license and authors as above), modified to expose a global `THREE` object.

Both are used under the terms of their original MIT licenses. `libs/react-simple.js` is original code written for this project (a minimal React-API-compatible shim) and is covered by this repository's own MIT license, not a third-party one.
